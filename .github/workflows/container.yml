name: Build and Push Docker image

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  build_and_push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: authenticate with Google Cloud
      uses: google-github-actions/auth@v2
      with:
        workload_identity_provider: "projects/${{ secrets.GCP_PROJECT_ID }}/locations/global/workloadIdentityPools/${{secrets.POOL_ID }}/providers/${{ secrets.PROVIDER_ID }}"
        service_account: ${{ secrets.SERVICE_ACCOUNT_NAME }}

    - name: Set up gcloud SDK
      uses: google-github-actions/setup-gcloud@v2

    - name: Info
      run: gcloud info

    # - name: Build and push Docker image
    #   uses: docker/build-push-action@v2
    #   with:
    #     push: true
    #     tags: <location>-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/<repository-name>/<image-name>:<tag>
    #     file: ./Dockerfile

